export interface deviconInterface {
  devicon: string // plain icon e.g. devicon-css3-plain-wordmark
  deviconWithText: string // icon with text e.g. devicon-css3-plain-wordmark
  hovering?: boolean // used to determine if user is hovering over the icon
  clicked?: boolean // used to determine if user is hovering over the icon
}